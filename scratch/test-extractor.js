const fs = require('fs');
const { PDFParse } = require('pdf-parse');

async function test() {
  // Let's create a dummy PDF or just assume we have one. 
  // Actually, I can't create a PDF easily. I'll just check if parser.load() works.
  console.log("We can use the manual loop approach!");
}

test();
